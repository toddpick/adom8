# Documentation for US-94

**Story:** Fix Autonomy to show the Text Value instead of the Number  
**Generated:** 2026-02-18T01:04:30.6881734Z

---

## Overview

This update addresses a specific usability issue in the Adom8 Dashboard, focusing on improving the display of the Autonomy field to show both the numeric value and its descriptive text.

---

## Changes Made

## Changes
- Modified the `getAutonomyText()` function in the dashboard JavaScript
- Updated the autonomy display logic to use a mapping function
- Ensured the display shows both number and text (e.g., '3- Review & Pause')

---

## API Documentation

### Autonomy Text Mapping Function
```javascript
function getAutonomyText(level) {
    const autonomyMap = {
        1: '1- Plan Only',
        2: '2- Code Only',
        3: '3- Review & Pause',
        4: '4- Auto-Merge',
        5: '5- Full Autonomy'
    };
    return autonomyMap[level] || level?.toString() || '—';
}
```

---

## Usage Examples

### Usage in Dashboard
```javascript
// Before
autonomyEl.textContent = workItem.autonomyLevel; // Displays: 3

// After
autonomyEl.textContent = getAutonomyText(workItem.autonomyLevel); // Displays: '3- Review & Pause'
```

---




## Migration Guide

## Migration Guide
1. No code changes required in backend services
2. Update dashboard to use `getAutonomyText()` when displaying autonomy levels
3. Verify display across different autonomy levels

---


## Configuration Changes

### Configuration
- No configuration changes required
- Autonomy mapping is hardcoded in the `getAutonomyText()` function

---

*Generated by Documentation Agent*  
*Timestamp: 2026-02-18T01:04:30.6881734Z*
