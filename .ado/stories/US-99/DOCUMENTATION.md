# Documentation for US-99

**Story:** Move Lock and Codebase Initialized controls next to Provision ADO  
**Generated:** 2026-02-19T09:42:45.5927364Z

---

## Overview

## US-99: Move Lock and Codebase Initialized Controls Next to Provision ADO

This story focuses on reorganizing the top navigation controls to improve user experience and control grouping in the ADOm8 dashboard. The changes involve moving the Lock and Codebase Initialized controls to sit next to the Provision ADO button.

---

## Changes Made

### HTML Structure Changes
- Moved `function-key-btn` from `.header-tools` to `.top-nav`
- Moved `codebase-badge` from `.header-tools` to `.top-nav`
- Preserved existing button IDs and click handlers
- Updated order in `.top-nav` to: Provision ADO → Lock → Codebase Initialized

### CSS Responsive Adjustments
- Added media query for top-nav controls at 1024px breakpoint
- Ensured controls remain fully clickable and visible across device sizes
- Implemented flex-wrap for mobile/tablet layouts

---

## API Documentation

### Button Handlers

#### `handleProvisionAdo()`
- **Description**: Provisions Azure DevOps artifacts
- **Trigger**: Click on `#provision-btn`
- **Behavior**: Prompts for confirmation, calls Azure Function with function key

#### `openFunctionKeyPrompt()`
- **Description**: Opens modal/prompt to set or update function key
- **Trigger**: Click on `#function-key-btn`
- **Behavior**: Allows setting/clearing function key for secured actions

#### `openScanModal()`
- **Description**: Opens codebase initialization modal
- **Trigger**: Click on `#btn-scan-codebase`
- **Behavior**: Displays codebase scan configuration and confirmation dialog

---

## Usage Examples

```html
<!-- Top Navigation Structure -->
<div class="top-nav">
    <button id="provision-btn" onclick="handleProvisionAdo()">🧩 PROVISION ADO</button>
    <button id="function-key-btn" onclick="openFunctionKeyPrompt()">🔓</button>
    <span id="codebase-badge" class="codebase-badge">
        <span id="codebase-badge-label">⚠ Codebase Not Initialized</span>
        <button id="btn-scan-codebase" onclick="openScanModal()">🔄</button>
    </span>
</div>
```

### Responsive Behavior
- Desktop (1024px+): All controls on one row
- Mobile/Tablet (<1024px): Wrapping allowed, preserving order

---




## Migration Guide

### Migration Steps
1. Ensure existing click handlers remain intact
2. Verify button IDs are preserved
3. Test responsiveness across different screen sizes
4. Validate function key and codebase initialization workflows

---


## Configuration Changes

### CSS Configuration
```css
@media (max-width: 1023px) {
    .top-nav {
        flex-wrap: wrap;
        padding: 8px 16px;
    }
    .top-nav .nav-spacer {
        flex-basis: 100%;
        height: 0;
    }
}
```

---

*Generated by Documentation Agent*  
*Timestamp: 2026-02-19T09:42:45.5927364Z*
