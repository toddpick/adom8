# Specification for {{ WORK_ITEM_ID }}

**Story:** {{ TITLE }}

> Authoritative spec for the Coding Agent. Derived from the story description, codebase context, and Planning analysis. Reference this to stay on track during implementation.

---

## Summary
{{ SPEC_SUMMARY }}

---

## Components & Interfaces Touched

{{ if SPEC_COMPONENTS && (SPEC_COMPONENTS | array.size) > 0 }}
{{ for component in SPEC_COMPONENTS }}
- {{ component }}
{{ end }}
{{ else }}
_No components identified by Planning._
{{ end }}

### Affected Files
{{ if AFFECTED_FILES && (AFFECTED_FILES | array.size) > 0 }}
{{ for file in AFFECTED_FILES }}
- `{{ file }}`
{{ end }}
{{ else }}
_None identified._
{{ end }}

---

## Expected Behavior

{{ if SPEC_BEHAVIORS && (SPEC_BEHAVIORS | array.size) > 0 }}
{{ for behavior in SPEC_BEHAVIORS }}
- {{ behavior }}
{{ end }}
{{ else }}
_No explicit behaviors enumerated; refer to story description and acceptance criteria._
{{ end }}

---

## Acceptance Criteria

{{ if SPEC_ACCEPTANCE && (SPEC_ACCEPTANCE | array.size) > 0 }}
{{ for criterion in SPEC_ACCEPTANCE }}
- [ ] {{ criterion }}
{{ end }}
{{ else }}
{{ ACCEPTANCE_CRITERIA }}
{{ end }}

---

## Non-Goals / Out of Scope

{{ if SPEC_NON_GOALS && (SPEC_NON_GOALS | array.size) > 0 }}
{{ for item in SPEC_NON_GOALS }}
- {{ item }}
{{ end }}
{{ else }}
_None specified._
{{ end }}

---

*Generated by Planning Agent*  
*Timestamp: {{ TIMESTAMP }}*
